[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "representative-statistic-steering"
version = "0.2.0"
description = "Target-conditioned perturbation steering for representative LLM statistics"
requires-python = ">=3.10"
dependencies = [
  "datasets",
  "numpy",
  "pandas",
  "pyyaml",
  "scikit-learn",
  "torch",
  "transformers",
]

[project.optional-dependencies]
dev = [
  "pytest",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
