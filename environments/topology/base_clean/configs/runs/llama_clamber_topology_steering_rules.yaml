extends: ./llama_clamber_topology_steering.yaml

runtime:
  log_dir: artifacts/logs/llama_clamber_topology_steering_rules

judge:
  provider: rules

clamber_topology_steering:
  output_dir: artifacts/reports/clamber_topology_steering_rules
